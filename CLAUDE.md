# CLAUDE.md

Read `AGENTS.md` first and follow it; it is the only source of repository rules.

Claude-specific: Swift package changes use `swift build` / `swift test` only; no xcodebuild or simulator.
